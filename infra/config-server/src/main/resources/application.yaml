spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations:
            - classpath:config/stats/{application}
            - classpath:config/core/{application}

server:
  port: 8888